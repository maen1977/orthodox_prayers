# إصلاح R20.2 — ترقية verified-data إلى عقد التسعة أيام

## السبب

كان فرع `verified-data` يحمل حزمة موقعة قديمة من ثمانية أيام. مسار Build كان يستوردها فوق مصدر 5.0.22 ثم يشغّل مدقق عقد التسعة أيام، فظهرت أخطاء السياسة وعدد الأيام وحقول اختيار القداس.

## الإصلاح

- إضافة فحص مسبق `validate_verified_data_contract.py` قبل استيراد بيانات الفرع في Build وRelease.
- رفض حزمة صحيحة التوقيع لكنها غير متوافقة مع عقد التسعة أيام، برسالة واحدة واضحة.
- منع `preserve_same_day_language_lanes.py` من استعادة حزمة قديمة ذات ثمانية أيام فوق مرشح جديد من تسعة أيام.
- إضافة اختبارات لعقد الاستيراد ولرفض baseline القديم.

## الترتيب المطلوب بعد رفع الملفات

1. شغّل **Rolling Week Update**.
2. اختر `mode = update`.
3. اترك التاريخ فارغًا ليستخدم تاريخ عمّان الحالي، أو أدخل التاريخ المطلوب.
4. بعد نجاحه، تأكد أن فرع `verified-data` نُشر بعقد 9 أيام.
5. أعد تشغيل **Build**.

لا يفيد إعادة تشغيل Build قبل تحديث `verified-data`؛ سيوقفه الفحص الجديد عمدًا برسالة `VERIFIED_DATA_UPGRADE_REQUIRED` بدل استيراد بيانات غير متوافقة.

## التحقق المحلي

- `352 passed`
- `validate_workflows.py` ناجح.
- جميع بوابات الجودة اللاحقة ناجحة عند تشغيلها منفردة.
