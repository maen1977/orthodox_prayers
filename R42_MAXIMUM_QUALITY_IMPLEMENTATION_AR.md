# تقرير تطوير Orthodox Prayers — مرحلة R42 القصوى

**التاريخ:** 2026-08-04  
**إصدار التطبيق:** 5.1.0 (`versionCode 50100`)  
**نطاق أندرويد المثبت:** Android 8.0 / API 26 فأحدث  
**حالة الروزنامة:** محمية وغير معدلة حتى 2050-12-31

## القيود التي تم الالتزام بها

1. لم يتم خفض `minSdk` عن API 26.
2. لم يتم تخفيف التوقيع أو HTTPS أو سياسة المصادر المسموح بها.
3. لم تُعدَّل بيانات الروزنامة أو تواريخها أو ملفاتها السنوية الممتدة حتى نهاية 2050.
4. التدقيق الديني بقي آليًا صارمًا داخل GitHub Actions، ولا يتطلب تدقيقًا بشريًا يوميًا.
5. عند غياب التذكار الموثق لا يظهر عنوان أو بطاقة أو نص بديل أو مساحة فارغة.
6. لم تُضف إعلانات أو تحليلات أو تتبع أو إطار واجهات ثقيل.

## ما تم تطويره

### 1. اختبار Android 8 منخفض الذاكرة

- إضافة مسار GitHub Actions مستقل على API 26.
- المحاكي مضبوط على RAM بسعة 1GB وHeap بسعة 192MB.
- نسخة Release لا تُبنى قبل نجاح مساري Android 8 وAndroid الحديث.
- تشغيل اختبار قارئ خفيف واختبار وصول مستقل.

### 2. قياس الأداء بحدود قبول واضحة

- قياس زمن التشغيل البارد بواسطة `am start -W`.
- قياس `TOTAL PSS` من `dumpsys meminfo`.
- ميزانية مستقلة لـAPI 26 وAPI 35 في ملف JSON قابل للمراجعة.
- فشل GitHub Action تلقائيًا عند تجاوز زمن التشغيل أو الذاكرة الحد المسموح.
- حفظ تقارير JSON ومخرجات Android الكاملة ضمن Artifacts.

### 3. إمكانية الوصول والخط الكبير

- اختبار الواجهة بالعربية واتجاه RTL.
- اختبار مقياس خط 1.65.
- التحقق آليًا من أن أهداف اللمس المرئية والقابلة للنقر لا تقل عن 48dp.
- رفع زر إظهار أدوات القارئ إلى 48dp.
- إبقاء زر الرجوع الأساسي على 48×48dp.

### 4. Baseline Profile خفيف

- إضافة ملف `app/src/main/baseline-prof.txt` لمسارات التشغيل والصفحة الرئيسية والقارئ والبحث والتقويم.
- القواعد محصورة داخل حزمة التطبيق ولا تشمل مكتبات عامة أو ملفات محتوى.
- إضافة `androidx.profileinstaller:profileinstaller:1.4.1` لمساعدة Android 8 على تثبيت الملف.
- إضافة فحص ثابت لحجم القواعد ونطاقها.
- إضافة فحص بعد بناء Release يثبت وجود `baseline.prof` و`baseline.profm` داخل APK وAAB.

### 5. حماية أقوى لتحديثات المحتوى

- كل Manifest جديد يولد بحقل `valid_until_utc`.
- مدة الصلاحية المسموحة من 6 إلى 72 ساعة، والافتراضي 48 ساعة.
- رفض Manifest المنشور من المستقبل أو المنتهي أو ذي نافذة غير منطقية.
- الحفاظ على توافق Manifests القديمة الموقعة التي لا تحتوي مدة صلاحية.
- منع الالتفاف إلى مسار مباشر عند فشل التوقيع أو السلامة أو الصلاحية.
- السماح بمسار التوافق فقط عند فشل توافر الشبكة أو HTTP قبل قبول Manifest لذلك اليوم.
- فصل السياسة الأمنية في `ManifestSecurityPolicy` لتقليل التكرار وتسهيل الاختبار.

### 6. فحص التذكار والمحتوى الديني

- استمرار التدقيق الصارم للرسالة والإنجيل ومراجع الآيات والبصمات لكل لغة.
- منع النص الناقص أو المكرر أو المعدل دون اكتشاف.
- منع الخلط بين العربية والإنجليزية واليونانية.
- التذكار غير الموثق أو الفارغ يعامل على أنه غير موجود.
- لا تظهر العبارة العامة القديمة على أنها تذكار حقيقي.
- الروزنامة تُفحص بالبصمة فقط ولا يعاد توليدها أو تعديلها.

### 7. بوابة ترجمة أكثر استقرارًا

- تحويل فحص Java من Regex واحد على جميع الملفات إلى فحص مستقل لكل ملف.
- يمنع ذلك التراجع البطيء والتوقف ضمن عمليات الاختبار الطويلة.
- إضافة مهلة 30 ثانية لاختبار الترجمة حتى يفشل بوضوح بدل تعليق الإصدار.

### 8. تجهيز Google Play

- إضافة Release Notes بالعربية والإنجليزية واليونانية.
- التحقق من الطول، عدم وجود Placeholders أو روابط، ووجود النص بالكتابة الصحيحة.
- تضمين الملاحظات الثلاث في حزمة Play Console.
- استمرار توليد الصور من اختبارات المحاكي وفحص سياسة الخصوصية وData Safety والصلاحيات.

### 9. حزمة مصدر نظيفة

- إصلاح منشئ ZIP ليشمل ملفات الهوية العامة فقط من `release/branding`.
- استمرار استبعاد Git history وCaches وملفات البناء والمفاتيح والتواقيع الخاصة.
- ZIP حتمي: نفس المصدر ينتج نفس البايتات والبصمة.

## نتائج التحقق

- **461 اختبار Python نجح.**
- **14 اختبارًا فرعيًا نجح.**
- بوابة الجودة الكاملة `run_quality_gate.py --strict-native-lanes` نجحت.
- العربية والإنجليزية واليونانية: تغطية تقنية 100%.
- التدقيق الديني الصارم: نجح.
- التوقيعات المنفصلة: نجحت.
- فحص الأسرار: لم يعثر على مفاتيح خاصة أو Keystores أو بيانات اعتماد مباشرة.
- Android SDK contract: API 26/36 ثابت، والإصدار محجوب بمساري API 26 و35.
- الروزنامة: 9,131 يومًا، 28 ملفًا، حتى 2050-12-31.
- بصمة قفل الروزنامة: `81604edecae2befa2263df01a2231ca3925b0a046a4383db9de9f2388edcebcf`.
- ملفات JSON للروزنامة في المصدر والأصول لم تتغير مقارنة بنسخة R41.
- فحص Java المستقل لـ`ManifestSecurityPolicy` نجح باستخدام `javac`.

## ما لم يمكن تنفيذه داخل بيئة العمل الحالية

لم يتم إنشاء APK أو AAB محليًا؛ بيئة التنفيذ لا تحتوي Android SDK ولا Gradle cache، والاتصال الخارجي لتنزيلهما محجوب. لذلك لا توجد مطالبة غير صحيحة بأن APK النهائي بُني أو شُغّل هنا.

تم تجهيز GitHub Actions لتنفيذ الأعمال التي تحتاج بيئة Android كاملة:

1. Unit Tests وLint.
2. بناء Debug APK وInstrumentation APK.
3. اختبار Android 8 بذاكرة 1GB.
4. اختبار Android الحديث والتقاط صور المتجر.
5. قياس Cold Start وPSS.
6. بناء APK وAAB موقعين.
7. التحقق من Baseline Profile والصلاحيات والتوقيع.
8. إنشاء SBOM وحزمة Play Console وبصمات SHA-256.

إنتاج ملفات Android النهائية يتطلب رفع هذه النسخة إلى مستودع GitHub وتشغيل Workflow `Build` مع أسرار التوقيع الصحيحة.
