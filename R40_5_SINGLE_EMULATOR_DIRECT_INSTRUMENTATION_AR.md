# تقرير R40.5 — تثبيت مسار Android دفعة واحدة

## سبب التغيير

سجلات GitHub أثبتت أن التطبيق كان يُبنى بنجاح، لكن محاكي API 29 يفقد خصائص الجهاز أثناء اكتشاف Gradle/DDMLib، فيظهر `Unknown API Level` وتفشل المهمة قبل تشغيل اختبارات التطبيق. تشغيل API 29 وAPI 35 كمصفوفة جعل واجهة GitHub تعرض مهمتين لنفس التطبيق، وأبقى الإصدار كله أحمر عند تعطل المحاكي القديم.

## التصميم النهائي

- التطبيق الناتج واحد فقط.
- توجد مهمة Runtime واحدة باسم واضح على Android 15 / API 35.
- توافق الأجهزة القديمة لا يعتمد على محاكي GitHub غير المستقر؛ يُثبت عبر:
  - `minSdk = 26`، ويشمل Android 8 فأحدث.
  - `lintDebug`.
  - `lintRelease`.
  - مدقق `validate_android_sdk_contract.py`.
- لم يعد الاختبار يستخدم `connectedDebugAndroidTest`.
- يبني GitHub ملف التطبيق وملف الاختبارات دون اكتشاف جهاز، ثم:
  1. يثبت APK التطبيق عبر ADB.
  2. يثبت APK الاختبارات عبر ADB.
  3. يتحقق من تسجيل `AndroidJUnitRunner`.
  4. يشغّل `am instrument` مباشرة.
  5. يرفض النشر عند أي فشل أو انهيار أو غياب نتيجة JUnit ناجحة.
  6. يسحب صور المتجر العربية والإنجليزية واليونانية.

## منع الأخطاء المتتابعة

أضيفت مهل مستقلة بدل مهلة واحدة قصيرة:

- انتظار الجهاز: 180 ثانية.
- أوامر ADB العادية: 30 ثانية.
- تثبيت كل APK: 180 ثانية.
- اختبارات Instrumentation: 1200 ثانية.
- سحب لقطات المتجر: 180 ثانية.

كما صُحح تفسير `INSTRUMENTATION_CODE: -1`؛ هذه القيمة تعني نجاح Android Instrumentation وليست خطأ.

## التشخيص التلقائي

عند أي فشل تُرفع تلقائيًا:

- قائمة أجهزة ADB.
- خصائص Android الكاملة.
- معلومات حزمة التطبيق.
- سجل Logcat.
- صورة الشاشة الأخيرة.
- سجل Instrumentation النصي.

## الاختبارات

- 425 اختبارًا ناجحًا.
- 14 اختبارًا فرعيًا ناجحًا.
- بوابة الجودة الكاملة: ناجحة.
- رزنامة 2026–2050: ناجحة.
- اللغات العربية والإنجليزية واليونانية: ناجحة.
- المصادر والتوقيع والصوم والخدمات: ناجحة.
- عقد Android: `minSdk=26`, `targetSdk=36`, `compileSdk=36`, ومحاكي Runtime واحد API 35.

## ما يحتاج GitHub فقط

بيئة العمل المحلية لا تستطيع تنزيل Gradle 8.13 من `services.gradle.org`، لذلك بناء Android الفعلي وتشغيل المحاكي سيُثبتان عند رفع R40.5 وتشغيل Build. جميع الفحوص المصدرية والعقود والاختبارات غير المعتمدة على تنزيل Gradle نجحت محليًا.
