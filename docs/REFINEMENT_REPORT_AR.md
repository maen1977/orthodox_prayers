# تقرير تنقيح نسخة المصدر

## ما تم إصلاحه

- إزالة سجل Git والكاش وملفات Python bytecode وملفات البناء من نسخة التسليم.
- حذف رسائل Commit وسكربتات التحقق الخاصة بالإصدارات القديمة.
- نقل تقارير الإصلاح وملاحظات الإصدارات التاريخية إلى `docs/history`.
- تفعيل `--strict-native-lanes` ليشغّل فحصًا حقيقيًا يمنع نسخ العربية إلى الإنجليزية أو اليونانية ويرفض الأبجدية الخاطئة.
- إبقاء اكتمال الإنجليزية واليونانية شرطًا منفصلًا لإصدار Production، دون اختلاق نصوص أو ترجمتها.
- استبدال Gradle Wrapper JAR وسكربتي Linux/Windows بالملفات الرسمية المتوافقة مع Gradle 8.9.
- إضافة `scripts/verify_gradle_wrapper.py` لفحص بصمات Wrapper والتوزيعة قبل البناء.
- نقل سر توقيع البيانات إلى GitHub Environment باسم `production-data-signing` في Workflow التحديث.
- تحسين مولّد ZIP ليكون حتميًا ويمنع مسارات الهروب والتسريبات والملفات السرية والكاش.
- تحديث README ووثائق الجاهزية والحماية.

## الفحوص المنفذة

- 76 اختبار Python: ناجحة.
- فحص Workflows: ناجح.
- فحص الأسرار: ناجح.
- التحقق من توقيعات بيانات اليوم: ناجح.
- فحص مخطط JSON والأصول المضمّنة: ناجح.
- فحص Gradle Wrapper 8.9: ناجح.
- بوابة المصدر مع `--strict-native-lanes`: ناجحة.

## قيود مقصودة ما زالت قائمة

- البيانات المضمّنة تحمل تاريخ 2026-07-14؛ لذلك تفشل بوابة `--require-current` بتاريخ 2026-07-16 كما ينبغي.
- حزم الإنجليزية واليونانية غير مكتملة؛ لذلك تفشل `validate_release_readiness.py` كما ينبغي.
- لم تُضف نصوص دينية غير موثقة، ولم تُستخدم ترجمة آلية لسد النقص.
- لم يُبنَ APK محليًا في بيئة التنقيح لعدم توفر Android SDK واعتماديات Gradle الشبكية؛ Workflow GitHub يبني Debug بعد الرفع.
