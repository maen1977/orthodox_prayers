# إصلاح التحديث اليومي داخل Workflow واحد

- بقي ملف GitHub Actions واحد فقط: `.github/workflows/church-prayers.yml`.
- وضع `build` يبني `Church-Prayers.apk` و`Church-Prayers.aab`.
- وضع `update` يعمل تلقائيًا وفق الجدول ويحدّث نافذة الأيام التسعة واللغات العربية واليونانية والإنجليزية.
- وضع `verify` يفحص فرع `verified-data` يدويًا عند الحاجة.
- التحديث اليومي يعيد استخدام مسار التوقيع والتحقق والنشر الذي كان موجودًا سابقًا، لكنه أصبح داخل نفس Workflow بدل ملف منفصل.
- الروزنامة الداخلية حتى نهاية 2050 لم تُعدّل.
