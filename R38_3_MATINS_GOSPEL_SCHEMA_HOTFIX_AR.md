# إصلاح R38.3 — اعتماد إنجيل السَحَر في مخطط البيانات

## سبب الفشل

أتمّ تحديث 2 أغسطس 2026 بناء نافذة الأيام التسعة، ونجحت القراءات والنصوص العربية والإنجليزية واليونانية، ثم توقف فحص JSON لأن القراءة `matins_gospel` لم تكن ضمن قائمة الأنواع المسموح بها في `schemas/daily_data.schema.json`.

المحرك والتطبيق كانا يدعمان إنجيل السَحَر مسبقًا، لذلك كان حذف القراءة أو تحويلها إلى `gospel` سيؤدي إلى فقدان المعنى الطقسي. الإصلاح الصحيح هو توحيد المخطط والحماية مع العقد الموجود.

## التعديلات

1. إضافة `matins_gospel` إلى أنواع القراءة المدعومة في مخطط JSON.
2. إخضاع إنجيل السَحَر لفحص المصدر الأصلي المستقل في العربية والإنجليزية واليونانية.
3. إخضاع نصه لبصمة SHA-256 وحالات التحقق نفسها المستخدمة للرسالة والإنجيل.
4. توسيع فحص عناوين ومراجع الواجهة ليشمل إنجيل السَحَر عند وجوده.
5. توسيع فحص البيانات المضمّنة وحماية أندرويد من النصوص غير الموثقة.
6. الإبقاء على `prokeimenon` و`epistle` و`gospel` كحد أدنى إلزامي يومي؛ إنجيل السَحَر اختياري، لكنه لا يُقبل ناقص الحماية إذا نُشر.
7. إضافة اختبارات قبول النوع الصحيح ورفض أي نوع قراءة مجهول.

## نتائج التحقق

- `401 passed, 14 subtests passed`.
- اختبار موجه لحزمة تحتوي `matins_gospel`: نجح مخطط JSON.
- فحص المسارات اللغوية الثلاثة للحزمة نفسها: نجح.
- فحوص التوقيع، المصادر الرسمية، الأردن، الصوم، الخدمات، واكتمال القداس: ناجحة.
- تعذر تشغيل Gradle محليًا بسبب عدم توفر DNS للوصول إلى `services.gradle.org`، وليس بسبب خطأ مصدر أو تجميع ظاهر.

## النتيجة المتوقعة على GitHub Actions

لن يتكرر الخطأ:

```text
readings/1/kind: 'matins_gospel' is not one of [...]
```

وسيكمل Update إلى فحوص التوقيع والنشر بدل التوقف عند مخطط JSON.
