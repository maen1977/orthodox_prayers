# إصلاح بقاء التطبيق على يوم الأحد

## السبب

النسخة المضمنة في التطبيق تحمل آخر نسخة احتياطية موقعة بتاريخ 2026-07-26. الخادم نشر حزمة موقعة تبدأ في 2026-08-05 وتغطي تسعة أيام حتى 2026-08-13. التطبيق القديم كان يشترط أن يساوي تاريخ بداية الحزمة تاريخ يوم فتح التطبيق حرفيًا؛ لذلك رفض الحزمة الصالحة عند فتحه في 2026-08-06 ورجع إلى النسخة الاحتياطية القديمة.

## الإصلاح

- البيان الموقّع ذو التغطية المتحركة يُقبل عندما يقع يوم عمّان الحالي داخل `start_date` و`end_date` شاملًا الطرفين.
- يبقى البيان القديم ذو اليوم الواحد ملزمًا بمطابقة التاريخ تمامًا.
- تغطية ملف اللغة يجب أن تطابق تغطية البيان الموقّع.
- بعد التحقق من التوقيع والبصمة والبنية، يختار التطبيق سجل يوم عمّان الحالي من `weekly_days`.
- تُرفض الحزمة إذا لم تحتوِ يوم اليوم، أو انتهت صلاحيتها، أو اختلفت تغطيتها، أو فشل توقيعها.

## ما يلزم على الهاتف

هذا إصلاح في كود التطبيق نفسه، لذلك يجب بناء APK جديد من Workflow **Build Church Prayers** وتثبيته فوق النسخة الحالية مرة واحدة. بعد ذلك يصبح فتح التطبيق كافيًا لإجراء فحص فوري للبيان الموقّع واختيار بيانات اليوم.

لا تحذف بيانات التطبيق عند التثبيت فوق النسخة الحالية الموقعة بالمفتاح نفسه.

## التحقق

- بوابة Church Prayers الكاملة: ناجحة.
- اختبارات المصدر الموجهة: 51 ناجحة.
- فحص Java النحوي لـ `UpdateManifest.java`: ناجح.
- روزنامة 2026-01-01 إلى 2050-12-31: 9,131 يومًا، 28 ملفًا، بلا تغيير.
- بصمة الروزنامة: `81604edecae2befa2263df01a2231ca3925b0a046a4383db9de9f2388edcebcf`.

تعذر تشغيل Gradle محليًا فقط لأن هذه البيئة لا تستطيع الوصول إلى `services.gradle.org`. Workflow GitHub يشغل `testDebugUnitTest` و`lintRelease` والبناء الفعلي.
