# R40.9 — إصلاح ذاتي لمسار البحث الآلي عن المصادر

## سبب الفشل

رسالة GitHub كانت:

```text
PIPELINE_PATCH_MISMATCH expected=R18.4 missing=scripts/source_window_research.py
```

الملف كان موجودًا داخل R40.8، لكن `scripts/update.py` كان يبحث داخله عن علامة العقد `AUTOMATED_FAIL_CLOSED`، بينما الملف لم يحتوِ هذه العلامة. لذلك ظهرت الرسالة وكأن الملف مفقود رغم أن المشكلة كانت علامة مفقودة.

## الإصلاح

1. إضافة `AUTOMATED_FAIL_CLOSED = True` إلى `scripts/source_window_research.py`.
2. تضمين نسخة مضغوطة من الملف داخل `scripts/update.py` ومثبتة ببصمة SHA-256.
3. إعادة الملف تلقائيًا وبشكل ذري إذا كان مفقودًا أو تالفًا.
4. إضافة الأمر:

```bash
python scripts/update.py --repair-pipeline-only
```

5. تشغيل أمر الإصلاح قبل مراحل Build وAndroid runtime وRelease وUpdate.
6. جعل رسالة التحقق تعرض `missing_files` و`missing_markers` كلًا على حدة.

## التحقق المنفذ

- حذف الملف من نسخة كاملة ثم تشغيل أمر الإصلاح: نجح وأعاد الملف بالبصمة المتوقعة.
- تشغيل التحديث الكامل بعد حذف الملف: نجح الإصلاح ووصل المسار إلى إعداد القراءات؛ توقف محليًا فقط عند تنزيل corpus لأن بيئة التنفيذ لا تملك اتصال DNS خارجي، بينما GitHub يملك الشبكة المطلوبة.
- 432 اختبارًا ناجحًا و14 اختبارًا فرعيًا عبر دفعتين نظيفتين.
- 61 اختبارًا موجهًا لمسار البحث والتحديث والإصدار.
- نجاح فحص Workflows وسياسة المقارنة والمصادر العامة والتوقيع والبيانات المضمنة واللغات وموارد Android وفحص الأسرار.

## بصمة الوحدة المضمنة

```text
4f536834cd6de05d3830cf6a41a000e91bfc55d9ad1aa8b25367a34140d4ffaf
```
