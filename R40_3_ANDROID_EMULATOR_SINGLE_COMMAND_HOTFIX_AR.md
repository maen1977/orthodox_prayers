# إصلاح R40.3: تشغيل محاكي أندرويد بأمر Bash واحد

## سبب الفشل

إجراء `ReactiveCircus/android-emulator-runner` نفّذ أسطر قيمة `script` كل سطر على حدة بواسطة `/usr/bin/sh -c`. لذلك وصل السطر `for attempt ...; do` إلى `sh` من دون أسطر جسم الحلقة و`done`، وانتهى التنفيذ بخطأ نحوي قبل بدء اختبارات Android.

## الإصلاح

- أزيلت كتلة الأوامر متعددة الأسطر من `.github/workflows/build.yml`.
- أصبح الـWorkflow يرسل أمرًا واحدًا فقط:

```yaml
script: bash scripts/run_android_emulator_ci.sh "${{ matrix.api_level }}"
```

- نُقلت العمليات إلى `scripts/run_android_emulator_ci.sh`:
  - انتظار اتصال ADB.
  - انتظار `sys.boot_completed=1`.
  - إعادة محاولة أوامر قطع Wi-Fi والبيانات.
  - تشغيل `connectedDebugAndroidTest`.
  - إعادة الشبكة حتى عند فشل الاختبار بواسطة `trap`.
  - سحب لقطات API 35 والتحقق منها.

## الحماية من رجوع الخطأ

أضيف اختبار يشغّل السكربت بواسطة:

```sh
/bin/sh -c "bash scripts/run_android_emulator_ci.sh 29"
```

مع ADB وGradle وهميين. هذا يحاكي طريقة استدعاء الـAction ويثبت أن الأمر الواحد لا يُقسم وأن ترتيب قطع الشبكة والاختبار والاستعادة صحيح.

## نتائج التحقق

- الاختبارات الكاملة: 423 ناجحًا و14 اختبارًا فرعيًا.
- اختبارات الإصلاح والعقود المرتبطة: 60/60.
- فحص Workflows: ناجح.
- جميع أوامر بوابة الجودة بعد الاختبارات: ناجحة.
- لا يوجد تعطيل أو تجاهل لفشل اختبارات Android.
