# R64.2 — إصلاح الآيات المحذوفة في نسخة المصدر ومزامنة Manifest

## سبب الخطأ

بعد توسيع القراءات حتى 2050 ظهر المرجع الليتورجي `ACT.15.5-34`. نسخة World English Bible تحتفظ برقم الآية 34 ولكن لا تحتوي متنًا معروضًا لها، وإنما ملاحظة اختلاف نصي فقط. الكود القديم حذف الآية الفارغة من الفهرس ثم اعتبر غيابها تلفًا في المصدر.

## ما تم إصلاحه

1. محلل USFM يميز الآن بين:
   - رقم آية موجود في المصدر لكن بلا متن بعد إزالة الحواشي = `numbered_source_omission`.
   - آية غير موجودة أصلًا من ملف المصدر = نقص حقيقي ويظل فشلًا.
   - أعضاء verse bridge مثل `3-4` = ليست omissions.
2. `ACT.15.34` أضيف أيضًا إلى قائمة الأمان التاريخية للإنجليزية، مع بقاء المرجع الكنسي كما هو `ACT.15.5-34`.
3. `fill_daily_from_native_corpora.py` صار يسمح فقط بالـomissions المعلنة من نفس manifest، ولا ينشر passage جزئيًا عند فقدان آية غير معلنة.
4. `validate_local_daily_engine.py` يمرر قائمة omissions حتى فحص نافذة الأيام.
5. Workflow يعيد إنشاء Scripture assets وmanifest بعد `build_internal_calendar_2050.py` مباشرة، وبالتالي أي مراجع جديدة أضافها bootstrap تدخل في manifest قبل Release Gate.
6. أضيف Cache مستقل لـ`.cache/scripture`.

## التحقق المحلي

- اختبار R64.2 + parser: 16/16 PASS.
- اختبارات R63/R64 والمحرك: 35/35 PASS.
- Release Gate النهائي: 155/155 PASS.
- `LOCAL_DAILY_RELEASE_GATE_OK`.
- التقويم المصدر: 9131 يومًا، 2026-01-01 إلى 2050-12-31.

## ملاحظة

R64.2 لا يضيف نصًا بديلًا للآية المحذوفة من نسخة المصدر ولا يغيّر المرجع الليتورجي. إذا كانت نسخة اللغة تحتوي النص، يبقى نصها كما هو؛ وإذا كانت النسخة نفسها تسجل الرقم بلا متن، يعرض التطبيق بقية المقطع من نفس النسخة دون اختراع كلمات.
