# ملاحظات إصدار Church Prayers 5.0.23

## R40.9 — إصلاح ذاتي لمسار البحث والمقارنة

- إصلاح سبب `PIPELINE_PATCH_MISMATCH`: الملف كان موجودًا، لكن علامة العقد `AUTOMATED_FAIL_CLOSED` لم تكن داخله.
- إضافة العلامة الصريحة إلى محرك مقارنة المصادر لتسعة أيام.
- تضمين نسخة مضغوطة ومثبتة ببصمة SHA-256 داخل `update.py` لإعادة الملف تلقائيًا إذا حُذف أو تلف.
- إضافة أمر `python scripts/update.py --repair-pipeline-only` وتشغيله قبل البناء والتحديث والإصدار المحمي.
- تحسين رسالة الخطأ لتفرّق بين ملف مفقود وعلامة عقد مفقودة.
- نجاح 432 اختبارًا و14 اختبارًا فرعيًا، إضافة إلى 61 اختبارًا موجهًا لمسار التحديث والإصدار.

## R40.8 — تثبيت مساحة القارئ عند فتح الأدوات

- إصلاح خلل حقيقي كان يجعل لوحة أدوات القراءة تستهلك معظم الشاشة ويترك نص الصلاة بارتفاع 17 بكسل فقط.
- وضع الأدوات داخل لوحة قابلة للتمرير لا تتجاوز 38% من ارتفاع الشاشة وبحد أقصى 340dp.
- حجز مساحة دنيا مقدارها 180dp للقارئ، مع استمرار الوصول إلى جميع الأدوات بالتمرير داخل لوحتها.
- تشديد اختبار Android ليشترط مساحة قراءة فعلية لا تقل عن 120dp مع صفوف ظاهرة وتمرير فعلي للنصوص الطويلة.
- نجاح 428 اختبارًا و14 اختبارًا فرعيًا وجميع مدققات الإصدار غير المعتمدة على تنزيل Gradle محليًا.

## R40 — البحث والمقارنة والإصدار الآلي

- مقارنة تسعة أيام تلقائيًا مع أولوية مصادر الأردن وبطريركية القدس.
- إيقاف النشر آليًا عند تعارض موثّق من نفس التقويم، من دون تخمين أو مراجعة بشرية يومية.
- كشف تغير بنية المواقع، وإعادة المحاولة، والاحتفاظ بآخر حزمة موقعة سليمة.
- اختبار Android على API 29 وAPI 35 مع قطع الشبكة داخل المحاكي.
- إنشاء لقطات متجر حقيقية بالعربية والإنجليزية واليونانية آليًا.
- فحص صلاحيات APK وتجميع حزمة Play Console وAAB ومواد المتجر والبصمات.
- فحص انتقالات السنوات والسنوات الكبيسة حتى نهاية 2050.

## إصلاح التحديث على الهاتف

عالج هذا الإصدار سبب ظهور رسالة «تعذر التحديث؛ تظهر آخر نسخة سليمة محفوظة» بعد تنزيل نافذة الأسابيع المقبلة. كان مخزن الهاتف يقبل 6 ميجابايت فقط، بينما التنزيل والبيان الموقّع يسمحان حتى 12 ميجابايت.

### التغييرات

- توحيد حد الحزمة الموقعة عند 12,000,000 بايت في البيان والتنزيل والحفظ والقراءة.
- رفع `versionCode` إلى 50023 و`versionName` إلى 5.0.23.
- جعل بيانات النشر الجديدة تتطلب 5.0.23 حتى لا تحمل النسخ القديمة ملفًا لا تستطيع حفظه.
- فتح التفاصيل التقنية تلقائيًا بعد فشل التحديث اليدوي.
- عرض رمز التشخيص الحقيقي ضمن رسالة الفشل.
- الإبقاء على آخر نسخة موقعة سليمة عند أي خطأ.

### لا تغيير في سياسات الأمان

التوقيع الرقمي وSHA-256 ومخطط JSON وفصل اللغات وسياسة fail-closed ما زالت إلزامية.


## توحيد الهوية

- اسم التطبيق المثبت أصبح `Church Prayers` في جميع لغات الجهاز.
- اعتماد أيقونة صليب أورشليم الجديدة للمشغّل والمتجر.
- أسماء ملفات APK/AAB وGitHub Artifacts أصبحت تبدأ بـ `Church-Prayers`.
- أضيف ملف ICO جانبي لاستخدامه في اختصارات ويندوز، مع بقاء أيقونة ملف APK في Explorer خاضعة لارتباط ملفات ويندوز.
## إصلاح فحص حجم الأيقونة في GitHub Actions

- إزالة الاعتماد غير المقصود على مكتبة `Pillow/PIL` من اختبار منطقة أمان الأيقونة.
- الإبقاء على الفحص الحقيقي للصورة باستخدام مكتبات Python القياسية فقط.
- يستمر الاختبار في التحقق من لوحة `108×108` ومن أن رسم الصليب لا يتجاوز `66×66` بكسل داخل المنطقة الآمنة.
- نجح الاختبار باستخدام `python -S` دون تحميل أي حزم خارجية.

