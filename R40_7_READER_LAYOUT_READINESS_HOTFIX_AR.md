# R40.7 — إصلاح جاهزية تخطيط القارئ في اختبار Android

## سبب الفشل

نجح GitHub في هذه المرة في تشغيل KVM ومحاكي Android 15، وتثبيت APK التطبيق وAPK الاختبارات، وتشغيل AndroidJUnitRunner. فشل اختبار واحد فقط:

`manualShowAndHideControlsNeverHidesReaderContent`

كان محول `RecyclerView` يحتوي المحتوى المطلوب، لكن الاختبار قرأ `getChildCount()` قبل أن ينتهي Android من قياس الصف الأول وربطه بالشاشة. لذلك ظهرت الرسالة:

`Reader has no visible child rows`

الاختبار الآخر الذي فتح نفس القارئ لاحقًا نجح، وكذلك اختبار لقطات المتجر، مما يثبت أن العطل سباق توقيت في الاختبار وليس فقدانًا لنصوص القارئ.

## الإصلاح

- استبدال الفحص اللحظي بعد `waitForIdleSync()` بفحص جاهزية محدود بمدة 12 ثانية.
- أخذ لقطة تشخيصية لحالة القارئ كل 100 مللي ثانية.
- لا يُعتبر القارئ جاهزًا إلا إذا تحققت الشروط معًا:
  - وجود `RecyclerView`.
  - وجود Adapter مرتبط.
  - عدد الصفوف لا يقل عن العدد المطلوب.
  - ارتفاع مقاس أكبر من صفر.
  - وجود صف مرئي واحد على الأقل.
  - إمكانية التمرير في النصوص الطويلة.
- طلب إعادة التخطيط فقط عندما يكون المحتوى موجودًا لكن الصفوف لم تُربط بعد.
- تطبيق الفحص نفسه قبل فتح أدوات القراءة وبعد فتحها وبعد إخفائها.
- الإبقاء على فحص أن فتح الأدوات لا يزيل القارئ وأن إخفاءها يعيد مساحة القراءة.
- عند تجاوز المهلة يفشل الاختبار بتفاصيل: وجود القارئ، وجود المحول، عدد الصفوف، الارتفاع، الحشو، عدد الصفوف المرئية، وإمكانية التمرير.

## ما لم يتغير

- لم تتغير نصوص الصلاة أو القداس.
- لم يتغير التقويم أو التحديث أو التوقيع.
- لم يتم تجاهل الاختبار أو تحويله إلى تحذير.
- لم يُستخدم `continue-on-error`.
- بقي اختبار عدم الاتصال ولقطات العربية والإنجليزية واليونانية إلزاميًا.

## نتائج التحقق

- 426 اختبارًا ناجحًا.
- 14 اختبارًا فرعيًا ناجحًا.
- 56 اختبارًا موجهًا لعقود الإصدار والمحاكي ناجحًا.
- جميع فحوص الجودة غير المعتمدة على Gradle ناجحة، بما فيها Workflow وSDK والتقويم حتى 2050 والمصادر والتوقيع واللغات والبيانات المضمنة.
- تعذر تجميع Android محليًا لأن بيئة العمل لا تستطيع الوصول إلى `services.gradle.org` لتنزيل Gradle 8.13؛ سيجري الإثبات الفعلي عند تشغيل Build على GitHub.
