# تقرير R40.8 — إصلاح مساحة القارئ عند توسيع أدوات القراءة

## العطل المثبت من تشغيل GitHub

نجح محاكي Android 15 في الإقلاع، ونجح تثبيت APK التطبيق وAPK الاختبارات، ثم فشل اختبار واحد فقط. سجل الاختبار أظهر أن القارئ كان موجودًا ومحمله متصلًا ويحتوي **630 صفًا**، لكن ارتفاع `RecyclerView` بعد فتح أدوات القراءة أصبح **17 بكسل** فقط. لذلك لم يكن السبب تأخرًا في الرسم، بل أن لوحة الأدوات ذات `wrap_content` أخذت تقريبًا كامل الارتفاع المتاح داخل التخطيط العمودي.

## الإصلاح في واجهة التطبيق

1. إضافة `MaxHeightScrollView` مخصص حول لوحة أدوات القراءة.
2. تحديد ارتفاع لوحة الأدوات بالأصغر من:
   - 38% من ارتفاع الشاشة.
   - 340dp.
   - المساحة التي يسمح بها الأب أثناء القياس.
3. جعل الأدوات قابلة للتمرير رأسيًا بدل دفع نص الصلاة خارج الشاشة.
4. إضافة حد أدنى 180dp لارتفاع القارئ كحماية إضافية للشاشات المدمجة.
5. إعادة لوحة الأدوات إلى بدايتها عند فتحها حتى تظهر الأدوات الأساسية أولًا.

## تشديد الاختبار

لم يتم إخفاء المشكلة أو تخفيف الاختبار. أصبح `ReaderSmokeTest` يشترط في كل حالة — قبل فتح الأدوات، أثناء فتحها، وبعد إخفائها — ما يلي:

- وجود `RecyclerView` وارتباط المحول.
- العدد الأدنى المطلوب من صفوف النص.
- ارتفاع قراءة فعلي لا يقل عن 120dp.
- وجود صفوف مرئية.
- قابلية التمرير للنصوص الطويلة.

## الملفات البرمجية المعدلة

- `app/src/main/java/com/orthodoxprayers/privateapp/ui/screens/ReaderScreen.java`
- `app/src/androidTest/java/com/orthodoxprayers/privateapp/ReaderSmokeTest.java`
- `tests/test_release_contract.py`
- `tests/test_r40_8_reader_controls_viewport.py`

## التحقق

- 428 اختبارًا ناجحًا.
- 14 اختبارًا فرعيًا ناجحًا.
- 44 مدقق إصدار ومحتوى وتوقيع وWorkflow ناجحًا.
- فحوص التقويم حتى 2050، اللغات، المصادر، التوقيع، البيانات المضمنة، الصوم والخدمات كلها ناجحة.
- تعذر تجميع Android محليًا فقط لأن البيئة لا تستطيع تنزيل Gradle 8.13 من `services.gradle.org`. التشغيل على GitHub هو الإثبات النهائي للمحاكي والـAPK.
