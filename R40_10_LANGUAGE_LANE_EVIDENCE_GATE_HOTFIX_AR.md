# R40.10 — إصلاح بوابة الأدلة لمسارات اللغات المستقلة

## سبب الفشل

نجح الملف الجامع في إثبات اكتمال النصوص بالعربية والإنجليزية واليونانية. بعد ذلك أعاد Workflow تشغيل البوابة على `ar.json` مع `--language ar`. لكن `validate_automated_religious_evidence.py` لم يمرر اختيار اللغة إلى `validate_daily_native_content.py` و`validate_scripture_translations.py`. لذلك تعامل المدقق مع الملف العربي المعزول كأنه ملف متعدد اللغات، وطالب داخله بأدلة إنجليزية ويونانية رغم أن عزل هذه الأدلة هو السلوك الصحيح للنشر.

## الإصلاح

- أضيف خيار `--language` إلى `validate_daily_native_content.py`.
- عند تحديد لغة، تُفحص المراجع والنصوص والبصمات والمصادر وبدائل الخدمة لهذه اللغة وحدها.
- عند عدم تحديد لغة، يستمر الفحص الصارم للغات الثلاث في الملف الجامع.
- أصبحت `validate_scripture_translations.py` تقبل `--language` وتمرره للمدقق الأصلي.
- أصبحت بوابة الأدلة تمرر اللغة إلى كلا المدققين.
- أضيف فشل واضح عند عدم تطابق `data.language` مع اللغة المطلوبة.

## التحقق

- 435 اختبارًا ناجحًا.
- 14 اختبارًا فرعيًا ناجحًا.
- 45 مدقق جودة غير معتمد على pytest ناجحًا.
- اختبار رجعي يثبت أن `ar.json` ينجح مع `--language ar` ويفشل عمدًا إذا عومل كملف متعدد اللغات.

## النتيجة

يبقى الملف الجامع مسؤولًا عن اكتمال اللغات الثلاث، بينما كل ملف لغة منشور يثبت اكتمال لغته الأصلية فقط دون خلط أو نسخ أدلة من المسارات الأخرى.
