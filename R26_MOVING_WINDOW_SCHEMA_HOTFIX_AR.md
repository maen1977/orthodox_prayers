# إصلاح R26 — عقد مخطط النافذة المتحركة

## المشكلة

كان مولّد التحديث يدعم نافذة متحركة من 9 إلى 42 يومًا، وكان التحقق البرمجي والتطبيق يدعمان ذلك، لكن مخطط `schemas/daily_data.schema.json` بقي مقيّدًا بعقد النسخة القديمة: ثمانية أيام مستقبلية فقط. لذلك نجح توليد 21 يومًا ثم فشل `validate_json_schema.py` بعرض محتوى قائمة `upcoming` كاملة.

## الإصلاح

- أصبح `schema_version = 10` يقبل من 8 إلى 41 يومًا مستقبلية، أي نافذة إجمالية من 9 إلى 42 يومًا.
- بقي `schema_version = 9` مقيدًا بسبعة أيام مستقبلية بالضبط للمحافظة على توافق الحزم القديمة الموقعة.
- يبقى التطابق الدقيق بين `rolling_week.day_count` وعدد عناصر `upcoming` مفروضًا بواسطة مدققات العقد البرمجية، لأن JSON Schema القياسي لا يدعم مقارنة طول مصفوفة بقيمة حقل آخر دون امتداد غير قياسي.
- أضيفت اختبارات سلوكية للنوافذ 9 و21 و42 يومًا، ولرفض الحدود الأقل والأعلى، ولعدم كسر عقد الإصدار 9.

## النتيجة

تمر حزمة 21 يومًا التي يولدها GitHub من فحص JSON Schema بدل أن تتوقف بعد اكتمال التوليد.
