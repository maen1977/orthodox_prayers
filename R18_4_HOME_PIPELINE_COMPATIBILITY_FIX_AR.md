# إصلاح توافق خط التحديث R18.4 مع R32

تم تحديث فحص التوافق داخل:

`scripts/update.py`

كان الفحص يبحث عن الوسم القديم `R15_THEME_PALETTE_IMPORT` داخل `HomeScreen.java`، بينما أزال تحسين R32 اعتماد `ThemePalette` والوسم القديم عمدًا. لذلك كان خط التحديث يتوقف برسالة `PIPELINE_PATCH_MISMATCH` رغم أن الصفحة الرئيسية صحيحة ومحدثة.

أصبح الفحص يعتمد على وسم العقد الحالي:

`R32_OWNER_UI_REFINEMENT`

وبذلك يتحقق خط R18.4 من وجود نسخة الصفحة الرئيسية الحديثة دون إعادة استيراد أو اعتماد واجهة قديم غير مستخدم.

كما أضيف اختبار انحدار يمنع عودة فحص `scripts/update.py` إلى وسم R15 المتقاعد.
