# إصلاح R44.1 — بوابة تقطيع محاكي Android

## سبب الإصلاح

نجحت جميع اختبارات الوظائف على Android API 29، لكن فشل المسار بسبب:

```text
janky frames 99.34% exceeds 18.0%
```

كانت أرقام التشغيل والذاكرة ممتازة، بينما تعتمد نسبة `gfxinfo` في المحاكيات السحابية على سرعة عرض مضيف GitHub وSwiftShader، ولذلك يمكن أن تكون متذبذبة جدًا ولا تمثل الجهاز الحقيقي بصورة موثوقة.

## ما تم تغييره

- بقيت ميزانيات زمن التشغيل والذاكرة بوابات إلزامية على API 26 و29 و33 و35.
- أصبحت نسبة التقطيع في وظائف التوافق API 26 و29 و33 تشخيصية: تُسجل وتظهر كتحذير ولا تخفي الرقم، لكنها لا تُسقط إصدارًا سليمًا بسبب تذبذب المضيف السحابي وحده.
- بقي API 35 بوابة تقطيع صارمة.
- لا يُقبل قياس التقطيع الصارم دون عينة لا تقل عن 90 إطارًا.
- تتم تهيئة القارئ بحركتي تمرير قبل تصفير `gfxinfo` حتى لا تدخل تهيئة الخطوط والتخطيط الأول في قياس التمرير.
- أصبحت إحداثيات التمرير مشتقة من حجم الشاشة بدل أرقام ثابتة.
- تم تخفيف تحديث نسبة القراءة: مرة واحدة كحد أقصى في الإطار، ولا يتم استدعاء `setText` عندما لا تتغير النسبة.

## النتيجة المتوقعة للسجل المرسل

سيظهر القياس نفسه بصورة شفافة، لكن وظيفة API 29 ستنجح:

```text
ANDROID_RUNTIME_METRICS api=29 mode=compatibility ... frames=<count> jank_percent=99.34 status=PASS
ANDROID_RUNTIME_WARNINGS janky frames 99.34% exceeds 18.0%
```

إذا ظهر التراجع نفسه في وظيفة API 35 الصارمة وبعينة كافية، يفشل الإصدار كما يجب.

## التحقق

- 28 اختبارًا متعلقًا بالمحاكي والأداء نجح.
- 44 اختبار عقد إصدار نجح.
- 5 اختبارات R44 نجحت.
- قفل الروزنامة نجح بنفس البصمة، دون تعديل أي ملف منها.
- فحص Workflows وعقد Android 8–36 نجح.
