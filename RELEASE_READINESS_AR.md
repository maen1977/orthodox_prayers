# جاهزية تطبيق الصلوات الكنسية 5.0.17

## نطاق الإصدار الفعلي

هذا الإصدار ليس مكتبة كتب كنسية، ولا يشترط تنزيل كل الخدمات الأرثوذكسية. هدفه المحدد هو:

- قداس اليوم في قارئ واحد متسلسل من الاستعداد إلى الختام.
- القراءات والقطع اليومية المتغيرة في مواضعها الصحيحة.
- صلوات الصباح والمساء والصلوات اليومية الأساسية.
- صلوات ما قبل المناولة والشكر بعدها عندما يتوفر نص أصلي معتمد في اللغة المختارة.
- بث مباشر وإعدادات قراءة بسيطة.

قائمة التدقيق التاريخية ذات الخمس عشرة خدمة باقية داخل المستودع لأغراض الأمان والمراجعة، لكنها ليست شرطًا لهذا المنتج، ولا يظهر عدّادها للمؤمن داخل التطبيق.

## الحالة الحالية

نطاق المصدر الخاص بالقداس المتسلسل يمر في بوابة الجودة الآلية:

- قالب قداس ثابت واحد لكل لغة.
- مواضع يومية لإنجيل السحر والبروكيمنن والرسالة والإنجيل.
- الدخول الصغير والدورة الكبرى والأنافورا والمناولة والختام.
- تمييز ما يقوله الكاهن سرًا وما يقوله المؤمن سرًا عندما يذكره المصدر.
- عدم ترجمة النصوص بين العربية والإنجليزية واليونانية.
- تحديثان يوميًا عند `01:00` و`06:00` بتوقيت `Asia/Amman`.
- منع تحديث السادسة من حذف نص صحيح قُبل في تحديث الواحدة.

ملف اليوم المضمّن في المصدر لقطة رجوع موقعة، وليس ادعاءً بأنه بيانات اليوم وقت بناء الحزمة. قبل بناء APK للنشر، يستورد مسار GitHub أحدث ملف يومي موقّع ثم يتحقق منه.

## المصادر والنصوص

- تعيين المناسبة ومراجع القراءات يأتي من مصادر أرثوذكسية مسجلة بحسب لغة القناة وتقويم القدس.
- نصوص القداس والصلوات لا تُركّب من ترجمة آلية، ولا تُنسخ من قناة لغة أخرى.
- نص القراءة الكتابية لا يظهر إلا مع بصمة ومصدر نص أصلي مسجل للغة نفسها؛ إذا غاب يبقى الموضع غير متوفر بدل التخمين.
- إثباتات الإذن الخاصة بالطبعات المقيدة تُحفظ خارج المستودع العام.

## أوامر الفحص

```bash
python scripts/run_quality_gate.py --strict-native-lanes
python scripts/validate_follow_along_liturgy.py
python scripts/validate_release_readiness.py
```

فحص قائمة الخدمات التاريخية يبقى متاحًا بصورة مستقلة ولا يتحكم بنطاق التطبيق الخفيف:

```bash
python scripts/validate_religious_completeness.py
```

## ملاحظة البناء

يحتاج بناء APK الفعلي إلى Android SDK وتوزيعة Gradle 8.13. يتحقق المستودع من بصمة Gradle Wrapper، وتشغّل بوابة GitHub بناء Debug وLint عند توفر بيئة Android الكاملة.
