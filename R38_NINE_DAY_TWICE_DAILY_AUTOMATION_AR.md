# R38 — نافذة تسعة أيام وتحديثان يوميًا

## القرار التنفيذي

- النافذة المنشورة ثابتة على 9 أيام: اليوم الحالي بتوقيت عمّان + ثمانية أيام.
- لا يعتمد الانتقال بين الأشهر أو السنوات على ملف سنة كامل؛ جميع الحدود تُحسب بالتاريخ.
- يعمل GitHub Actions مرتين يوميًا بتوقيت `Asia/Amman`:
  - 04:23
  - 16:43
- اختيرت الدقائق بعيدًا عن بداية الساعة لتقليل ازدحام الجدولة.
- يحتفظ التطبيق بآخر حزمة صحيحة، ولا يستبدلها بحزمة ناقصة أو غير موقعة.

## ما تم تعديله

1. تثبيت `ORTHODOX_ROLLING_WINDOW_DAYS` على 9.
2. جعل الحد الأدنى والأقصى للنافذة 9 لمنع عودة التضارب 9/21.
3. تعديل Workflow للتشغيل مرتين يوميًا فقط.
4. تعديل WorkManager في أندرويد لفحص نافذتي النشر.
5. تعديل سياسة الاستئناف لاكتشاف نشر الصباح أو المساء دون تكرار غير ضروري.
6. تحديث العقود الطقسية، عقد المحتوى الأصلي، والتوثيق العربي.
7. تحديث اختبارات النافذة، الصيام، التحديث، والعقود.
8. إبقاء شبكة المصادر الحالية بنظام الأولوية والفشل المغلق وعدم الترجمة أو التأليف الآلي.

## التحقق المنفذ

- `python -m pytest -q`: ناجح — 398 اختبارًا و14 اختبارًا فرعيًا.
- `python scripts/validate_workflows.py`: ناجح.
- `python scripts/validate_follow_along_liturgy.py`: ناجح.
- `python scripts/validate_full_liturgy_services.py`: ناجح.

## ملاحظة البناء

تعذر تشغيل Gradle في بيئة الفحص لأن Gradle Wrapper احتاج تنزيل `gradle-8.13-bin.zip` ولم تتوفر شبكة DNS. يجب أن ينفذ GitHub Actions أو جهاز التطوير:

```bash
./gradlew clean test lint assembleRelease --no-daemon
```

قبل رفع الإصدار إلى Google Play.

## إصلاح R38.1

- وُحّدت العقود المتبقية التي كانت تسمح بـ21–42 يومًا على تسعة أيام فقط.
- أصبحت حزمة 21 يومًا القديمة مرفوضة بوضوح من عقد البيانات المنشورة.
- صُححت صياغة موعدي التحديث في العربية والإنجليزية واليونانية.
- نجحت جميع مكونات بوابة الجودة الصارمة منفردة.
