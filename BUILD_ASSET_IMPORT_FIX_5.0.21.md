# إصلاح بناء التطبيق وحماية المكتبات الثابتة — 5.0.21

كان Workflow البناء ينسخ مجلدات `data` كاملة من فرع `verified-data` فوق ملفات `main`، بما فيها مكتبات الخدمات الثابتة. لذلك كان إصلاح تصنيف `small_compline` يُستبدل قبل تجميع APK ويعود الخطأ نفسه.

الإصلاح في 5.0.21:

- يستورد Build البيانات اليومية الموقعة من `verified-data`.
- يعيد فورًا مكتبات الخدمات البنيوية من `main` قبل الاختبارات والتجميع.
- يحمي `data/services/library.json` و`data/services/native/` ونسخها داخل Android assets.
- يشغل اختبار محاذاة تصنيفات الخدمات بعد الاستيراد.
- يمنع خروج APK يحمل رقم إصدار جديدًا مع مكتبات قديمة.
