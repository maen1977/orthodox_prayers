# تقرير R43 — التأهيل الأقصى للإصدار

## هوية الإصدار

- اسم التطبيق: Church Prayers
- الإصدار: `5.2.0`
- رقم الإصدار: `50200`
- الحد الأدنى: Android 8.0 / API 26
- الهدف وبيئة البناء: API 36
- لا إعلانات، لا تحليلات، ولا مكتبات واجهة ثقيلة جديدة.

## القيود الثابتة التي تم احترامها

1. لم يُخفض `minSdk` عن 26.
2. لم تُضعف حماية HTTPS أو توقيع البيانات أو سياسة الفشل المغلق.
3. لم يُعدّل أي ملف من ملفات الروزنامة حتى نهاية 2050.
4. بقي التذكار اختياريًا: يظهر فقط عند وجود قيمة موثقة، ويختفي بالكامل عند غيابها.
5. بقي التدقيق الديني آليًا داخل بوابات GitHub دون اشتراط مراجعة بشرية.

## ما تم تنفيذه في R43

### توافق وترقية

- مصفوفة محاكيات آلية على API 26 و29 و33 و35.
- Android 8 بذاكرة 1GB كشرط إصدار إلزامي.
- اختبار ترقية حقيقي يثبت الإصدار السابق، يزرع اللغة والوضع الليلي والمفضلة وموضع القراءة والبحث، ثم يثبت الإصدار الجديد باستخدام `adb install -r` ويتحقق من بقاء البيانات.
- اختبار إعادة التثبيت مع حفظ بيانات المستخدم.

### الفشل والاستعادة

- اختبار التشغيل دون إنترنت.
- اختبار إغلاق العملية وإعادة تشغيلها.
- اختبار Generation محلي تالف وتوقيع تالف، والتأكد من الرجوع للبيانات المضمنة الموثوقة.
- استمرار سياسة Last Known Good وعدم قبول البيانات غير الموقعة.

### الأداء والخفة

- قياس التشغيل البارد والدافئ.
- قياس PSS للصفحة الرئيسية والقارئ والبحث.
- قياس تقطيع القارئ عبر `gfxinfo`.
- ميزانيات منفصلة لـAPI 26 و29 و33 و35.
- ميزانية قصوى لحجم APK وAAB والنسخة المصدرية.
- Baseline Profile خفيف لمسارات التشغيل والقارئ والبحث والتقويم.
- عرض مركزي محدود إلى 840dp على الأجهزة اللوحية دون تغيير تخطيط الهواتف أو إضافة مكتبة جديدة.

### تجربة الاستخدام

- اختصارات Launcher للقراءات اليومية والقداس وصلاة الصباح.
- الاختصارات مترجمة بالعربية والإنجليزية واليونانية.
- اختبار عرض التذكار في اللغات الثلاث: لا عنوان ولا بطاقة ولا مساحة فارغة عند غيابه.
- استمرار أهداف اللمس وإمكانية الوصول والخط الكبير وRTL ضمن بوابة Android 8.

### الإصدار والمتجر

- Workflow محمي لبناء APK/AAB الموقّعين.
- Workflow منفصل لـGoogle Play Internal Testing.
- النشر الحقيقي إلى Play لا يعمل إلا عند تفعيل المدخل `publish` واستخدام بيئة محمية وسر `PLAY_SERVICE_ACCOUNT_JSON`.
- Dry-run كامل للتحقق من AAB وملاحظات الإصدار قبل الاتصال بالمتجر.
- ملاحظات إصدار 5.2.0 بالعربية والإنجليزية واليونانية.
- Workflow صحي أسبوعي يعيد تشغيل الجودة والبناء ويفتح Issue واحدة قابلة للتنفيذ عند الفشل.

### استقرار بوابة الاختبارات

- تشغيل جميع ملفات الاختبار في عمليات pytest معزولة وحتمية بدل إبقاء 470 اختبارًا في عملية طويلة تتأثر بالحالة المشتركة لبعض اختبارات الإصلاح.
- لا يتم حذف أو تجاوز أي اختبار؛ التقسيم يغيّر العزل فقط.

## نتائج التحقق المحلي

- اختبارات Python: 470 ناجحة.
- الاختبارات الفرعية: 14 ناجحة.
- تغطية الحزم الأصلية: العربية والإنجليزية واليونانية 100% تقنيًا.
- التدقيق الديني الصارم: ناجح.
- توقيعات البيانات: ناجحة.
- فحص الأسرار: ناجح.
- ملفات المتجر واللغات والموارد: ناجحة.
- Baseline Profile: 24 قاعدة، ناجح.
- الروزنامة: 9,131 يومًا، 28 ملفًا، من 2026-01-01 إلى 2050-12-31.
- بصمة قفل الروزنامة:
  `81604edecae2befa2263df01a2231ca3925b0a046a4383db9de9f2388edcebcf`

## ما لم تستطع بيئة التنفيذ المحلية إنجازه

- لم يُنتج APK أو AAB محليًا؛ Gradle Wrapper حاول تنزيل Gradle 8.13 لكن DNS الخارجي محجوب وظهر `UnknownHostException: services.gradle.org`.
- لم يُنشر AAB فعليًا إلى Google Play لعدم توفر حساب خدمة Play وسر النشر في هذه البيئة. مسار Dry-run نجح.
- لم تُنفذ شهادة أجهزة مادية لأن البيئة لا تحتوي هواتف متصلة. تم تجهيز نموذج الأدلة ومصفوفة المحاكيات واشتراطات التأهيل.
- لم تُرفع التغييرات إلى GitHub: المستودع المتصل أقدم من R42، ومحاولة Git Data API لإنشاء شجرة تغييرات رُفضت بصلاحية 403. لم يتم إنشاء فرع أو PR ناقص.

## الإجراء عند رفع النسخة إلى GitHub

1. استبدال محتويات المستودع بمحتويات الحزمة النظيفة، مع الحفاظ على أسرار GitHub خارج المصدر.
2. فتح فرع للإصدار وتشغيل Workflow `Build`.
3. لا يُدمج الفرع إلا بعد نجاح Android 26/29/33/35، الترقية، الاستعادة، Lint، Unit Tests، التدقيق الصارم، والميزانيات.
4. بعد نجاح Artifact الموقّع، تشغيل `Play Internal Testing` أولًا بوضع `publish=false`، ثم `publish=true` داخل البيئة المحمية.
5. تعبئة نتائج الأجهزة المادية في ملف التأهيل قبل الإنتاج العام.
