# R40.2 — إصلاح اختبار القارئ دون اتصال

## السبب

اختبار Android كان يفتح `next_sunday_full_liturgy` بالمعرّف غير المؤرخ بعد قطع الإنترنت.
هذا المعرّف لا يُعرض عندما تكون الحزمة المضمّنة أقدم من تاريخ الجهاز، التزامًا بسياسة منع عرض بيانات يومية قديمة على أنها حالية.
لذلك أعاد `ReaderScreen` صفحة عدم توفر بدل `RecyclerView`، وفشل الاختبار برسالة `Reader RecyclerView was not found`.

## الإصلاح

- يستخدم اختبار العرض دون اتصال معرّف الخدمة المؤرخ للحزمة المضمّنة عبر `DataRepository.datedServiceId`.
- بقي اختبار مستقل للمعرّف الحالي غير المؤرخ، ويعمل تلقائيًا عندما تكون الحزمة الموقعة مطابقة لتاريخ الجهاز.
- ينتظر Workflow اكتمال إقلاع Android قبل قطع الشبكة.
- تُعاد محاولة أوامر تعطيل Wi-Fi والبيانات خمس مرات، ولا يُسمح ببدء الاختبار إذا فشل قطع الشبكة.

## النتيجة المتوقعة

يختبر المحاكي نصًا ديناميكيًا حقيقيًا من الحزمة الموقعة دون الادعاء أن الحزمة القديمة هي بيانات اليوم، ويصبح اختبار عدم الاتصال فعليًا ومستقرًا على API 29 وAPI 35.

## التحقق

- اختبارات Python: 420 ناجحة و14 اختبارًا فرعيًا.
- فحص Workflow: ناجح.
- فحوص الرزنامة حتى 2050 والمصادر واللغات والتوقيع والمحتوى: ناجحة.
- تعذر تجميع Android محليًا لأن بيئة العمل لا تستطيع حل اسم `services.gradle.org` لتنزيل Gradle 8.13؛ GitHub Actions هو مكان اختبار المحاكي النهائي.
