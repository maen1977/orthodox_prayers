# إصلاح R37.1 لتجميع سياسة التحديث اليومية

## المشكلة
بعد توحيد جدولة التحديث إلى 06:07 صباحًا، بقي `RefreshPolicy.java` يشير إلى الثابتين القديمين `FIRST_REFRESH_HOUR` و`SECOND_REFRESH_HOUR` اللذين أزيلا من `UpdateCoordinator.java`. أدى ذلك إلى فشل `compileDebugJavaWithJavac`.

## الإصلاح
- توحيد منطق الاستئناف على نافذة نشر واحدة عند 06:07 بتوقيت عمّان.
- استخدام `DAILY_REFRESH_HOUR` و`REFRESH_MINUTE` من `UpdateCoordinator`.
- تحديث اختبار الوحدة وعقود Python القديمة لتتوافق مع الجدولة اليومية الواحدة.
- تحديث فحص R17 التاريخي وفحص `update.py` حتى لا يطلبا علامات الجدولة القديمة.

## السلوك النهائي
- GitHub Actions ينشر مرة يوميًا عند 06:07 بتوقيت الأردن.
- التطبيق يفحص عند الفتح.
- فحص الاستئناف الإضافي يعمل مرة واحدة بعد نافذة 06:07 إذا كان آخر فحص أقدم منها.
