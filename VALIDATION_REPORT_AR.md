# تقرير التحقق — 30 يوليو 2026

الإصدار: `5.0.22`  
عقد التحديث: `ROLLING_FUTURE_WINDOW` schema v2  
المدى الافتراضي: `21` يومًا  
المدى المدعوم: `9–42` يومًا  
المنطقة الزمنية: `Asia/Amman`

## ناجح

- `python -m pytest -q` → `354 passed`.
- `scripts/validate_workflows.py`.
- `scripts/verify_gradle_wrapper.py`.
- `scripts/scan_repository_secrets.py`.
- `scripts/verify_static_texts.py`.
- فحوص موارد Android وواجهات اللغات الثلاث.
- فحوص حزم الصلاة الأصلية والقارئ والقداس الكامل واختيار الخدمة.
- فحوص المصادر والقراءات والتوقيع والجدول والصيام.
- التوقيعات المنفصلة للبيانات المضمنة الحالية.

## اختبار التكامل

شُغّل تحديث غير موقّع في نسخة مؤقتة ابتداءً من `2026-07-30`، ونجح في إنشاء والتحقق من أيام مستقبلية متتالية، مع إعادة استخدام cache الموثوق. بيئة التنفيذ المحلية أوقفت العملية الطويلة بمهلة خارجية أثناء متابعة بقية الأيام؛ لم يسجل أي validator فشلًا قبل الإيقاف.

## بناء Android

لم يمكن تنزيل توزيع Gradle 8.13 في بيئة الفحص بسبب عدم توفر DNS/الإنترنت، لذلك لا يوجد ادعاء محلي بأن APK/AAB بُني هنا. Workflow المرفق ينفذ Unit Tests وLint وDebug APK على GitHub، ويمنع الإصدار الموقّع إن فشل أي شرط.
