# تقرير R40 — البحث والمقارنة والإصدار الآلي الكامل

## الهدف المنفذ

تحويل ما تبقى من دورة التحديث والنشر إلى مسار آلي داخل GitHub Actions، من دون اشتراط مراجع بشري يومي، ومن دون السماح للنظام بتخمين نص ديني أو ترجمة نص غير متوفر.

يبقى التطبيق موجّهًا للمستخدم ضمن نافذة ثابتة من تسعة أيام، مع رزنامة أمان داخلية من 2026 حتى نهاية 2050، وتحديثين يوميًا عند 04:23 و16:43 بتوقيت `Asia/Amman`.

## 1. محرك البحث والمقارنة بين المصادر

أضيفت سياسة مركزية في:

`canonical/source_comparison_policy.json`

وتفرض ما يلي:

- الأولوية للمصدر الأرثوذكسي الأردني في المعلومات المحلية.
- مرجعية تقويم بطريركية القدس والتقويم القديم.
- استخدام المصادر الأرثوذكسية المؤرخة مثل GOARCH وOCA للمقارنة، لا لفرض تقويم جديد على الأردن.
- مقارنة الرسالة والإنجيل والمناسبة والصوم والخدمة المعينة لكل يوم.
- التعارض مع مصدر الأردن، أو تعارض من نفس تقويم القدس، يوقف النشر تلقائيًا.
- اختلاف مصدر يتبع تقويمًا جديدًا يُسجل كتحذير مقارن ولا يطغى على الأردن والقدس.
- المصدر غير المؤرخ لا يستطيع إنشاء تعارض كاذب.
- عدم نسخ النصوص الكتابية الطويلة أو السنكسار الكامل من صفحات الويب؛ تُحفظ المراجع والحقائق القصيرة والبصمات وروابط الأدلة فقط.

المحرك التنفيذي:

`scripts/source_window_research.py`

وينتج لكل تشغيل:

- تقرير JSON لكل يوم من الأيام التسعة.
- تقرير `current.json` للحالة الحالية.
- ملخص Markdown داخل GitHub Actions.
- قرار آلي: نشر متعدد المصادر، نشر بمرجعية السلطة، نشر بخط أساس داخلي آمن، أو حجب بسبب تعارض.
- بصمة بنية الصفحة لكل موصل للمساعدة في اكتشاف تغير تصميم الموقع.

## 2. شبكة الموصلات ومقاومة تعطل المواقع

تم توسيع سجل الموصلات إلى عشرة موصلات، مع إضافة مصدر GOARCH المؤرخ بالشهر للمقارنة حسب التاريخ.

كل موصل يحدد:

- نوع التقويم.
- نطاق التاريخ الذي يستطيع خدمته.
- دوره: مرجعية محلية، تأكيد تقويم، مقارنة مؤرخة، دليل لغة، أو توفر فقط.
- عدد محاولات الاتصال والمهلة وحجم الاستجابة.
- بصمة بنية المحتوى.

أضيفت إعادة المحاولة التدريجية، وتخزين مؤقت للرابط نفسه داخل التشغيل حتى لا يُطلب المصدر الشهري تسع مرات، ومقارنة حالة الموصلات مع آخر نشر لاكتشاف الانحدار أو تغير بنية الصفحة.

## 3. بوابة القرار الديني الآلية

أضيفت:

- `scripts/validate_source_comparison.py`
- `scripts/validate_automated_religious_evidence.py`
- `scripts/validate_source_comparison_policy.py`
- `scripts/compare_source_snapshots.py`

وتتحقق من:

- وجود تسعة أيام متتالية.
- وجود المرجع الكتابي والنص الأصلي وبصمته في كل لغة.
- عدم استعمال ترجمة آلية أو تشكيل آلي.
- سلامة تقويم المصدر وتاريخ الدليل.
- عدم وجود تعارض محلي أو من نفس التقويم.
- بقاء نوع القداس والخدمة المعينة مطابقين للرزنامة الداخلية.
- عدم استعمال قداس بديل خاطئ عند غياب الطقس المعين.

لا توجد خطوة انتظار لمراجع يومي. إذا لم يستطع النظام اتخاذ قرار آمن، تكون النتيجة حجب النشر مع الاحتفاظ بآخر حزمة موقعة صحيحة.

## 4. تحديث GitHub Actions

### Update

يعمل يدويًا أو مرتين يوميًا فقط:

- 04:23 صباحًا.
- 16:43 مساءً.
- بتوقيت عمّان.

المسار يقوم تلقائيًا بـ:

1. فحص المصادر والموصلات.
2. بناء نافذة التسعة أيام.
3. تكوين قنوات العربية والإنجليزية واليونانية بصورة مستقلة، واشتراط نجاح القنوات الثلاث معًا قبل النشر.
4. تشغيل المقارنة والقرار المغلق عند التعارض.
5. مقارنة بصمات المصادر مع آخر نشر.
6. الحفاظ على القنوات الصحيحة من النشر السابق إذا كان التحديث الجديد أقل اكتمالًا.
7. استعادة مفتاح توقيع البيانات بعد انتهاء الاتصالات الخارجية فقط.
8. توقيع الحزمة والـmanifest والقنوات.
9. فحص نسخة العمل ونسخة commit ونسخة clone والرابط المنشور.
10. نشر فرع `verified-data` فقط بعد نجاح جميع البوابات.
11. فتح Issue عند الفشل وإرفاق ملخص المقارنة، وإغلاق تنبيه الفشل عند التعافي.

### Build

المسار يقوم تلقائيًا بـ:

1. بوابة Python والجودة واللغات والتوقيعات.
2. بناء Debug واختبارات الوحدة وLint.
3. اختبار Android على محاكي API 29 وAPI 35.
4. قطع Wi‑Fi وبيانات الهاتف داخل المحاكي لاختبار العمل دون إنترنت.
5. تشغيل اختبارات واجهة القارئ والخدمات.
6. إنشاء صورتين فعليتين لكل لغة: العربية والإنجليزية واليونانية.
7. منع Release إذا لم توجد الصور الست أو لم تجتز فحص PNG والأبعاد.
8. استيراد آخر `verified-data` موقّع قبل Release.
9. بناء APK وAAB موقعين.
10. استخراج صلاحيات APK وفشل الإصدار عند ظهور إذن حساس أو غير مسجل.
11. إنشاء حزمة Play Console تحتوي AAB، الأيقونة، Feature Graphic، الصور، القوائم الثلاثية، سياسة الخصوصية، Data Safety، الحقوق، والبصمات.
12. إنشاء SBOM وأرشيف مصدر نظيف وبصمات إصدار.

## 5. اختبارات Android ولقطات المتجر

أضيف:

`app/src/androidTest/java/com/orthodoxprayers/privateapp/StoreScreenshotTest.java`

وهو يختار كل لغة بصورة مستقلة، يفتح الواجهة الحقيقية، ويلتقط:

- الصفحة الرئيسية.
- صفحة القراءات.

وتُسحب الصور من محاكي API 35 إلى حزمة المتجر. لا توجد صور تجريبية أو صور منشأة خارج التطبيق داخل المشروع.

## 6. أتمتة Google Play والأمان

أضيفت:

- `scripts/build_play_store_release_package.py`
- `scripts/validate_play_store_metadata.py`
- `scripts/validate_release_permissions.py`

صلاحيات الإصدار المسموحة محدودة بالاتصال وحالة الشبكة والإشعارات والتذكيرات والطاقة. أذونات الموقع والكاميرا والميكروفون والاتصالات والرسائل والتخزين الواسع تمنع الإصدار.

لا يُقبل بريد دعم وهمي. يجب توفير `PLAY_SUPPORT_EMAIL` كسر حقيقي في بيئة `production`.

## 7. اختبارات الرزنامة حتى 2050

أضيف:

`scripts/validate_calendar_boundaries_2050.py`

ويفحص:

- جميع الانتقالات الأربع والعشرين بين السنوات 2026–2050.
- ست نوافذ حول السنوات الكبيسة.
- 30 نافذة من تسعة أيام، تضم 270 يومًا.
- عدم انقطاع التاريخ عند نهاية ديسمبر أو فبراير.

## 8. سياسة الخصوصية ومواد المتجر

تم تحديث العربية والإنجليزية واليونانية لتوضيح:

- الرزنامة الداخلية حتى 2050.
- المقارنة الآلية للمصادر.
- الاحتفاظ بآخر حزمة موقعة.
- عدم جمع بيانات المستخدم أو إرسالها إلى المصادر الكنسية.
- إنشاء لقطات المتجر من التطبيق الحقيقي داخل المحاكي.

## 9. نتائج التحقق المحلية

- `420 passed, 14 subtests passed`.
- سياسة المقارنة: 9 أيام، 10 موصلات، قرار آلي مغلق، ولا مراجع يومي.
- الرزنامة: 9,131 يومًا و25 سنة.
- حدود السنوات: 24 انتقالًا و6 نوافذ كبيسة.
- اللغات الأصلية: العربية والإنجليزية واليونانية 100% وفق عقود المشروع.
- الخدمات: 45/45 مكتملة تقنيًا، 38/45 طبعات أصلية واحدة، و7/45 تجميعات أصلية موثقة.
- فحوص التوقيع والمخطط والبيانات المضمنة والمصادر والصوم والخدمات والخصوصية والمتجر نجحت.
- فحص المستودع لم يجد مفاتيح خاصة أو Keystore أو بيانات اعتماد حرفية.

تعذر تشغيل Gradle محليًا لأن بيئة العمل لم تستطع الوصول إلى `services.gradle.org`. لذلك لا يُدّعى أن المحاكي أو APK/AAB عملا محليًا؛ الكود والـWorkflows موجودان، ويثبت GitHub Actions التنفيذ الفعلي بعد رفع R40 وتوفير الأسرار.

## 10. ما يبقى خارج قدرة الكود

هذه ليست مراجعة بشرية للمحتوى، وإنما بيانات وصول وقرارات حساب خارجية لا يمكن اختراعها:

- `DATA_SIGNING_PRIVATE_KEY_B64`.
- أسرار توقيع Android الأربعة.
- `PLAY_SUPPORT_EMAIL` الحقيقي.
- تفعيل GitHub Pages.
- الضغط على إجراءات Play Console وإقرارات الحساب القانونية.

لا يدّعي المشروع اعتمادًا كنسيًا رسميًا. إلغاء المراجعة البشرية اليومية يعني أن النشر يعتمد على الأدلة والبصمات والقواعد والتعارض المغلق، وليس أن التطبيق حصل على شهادة كنسية.
