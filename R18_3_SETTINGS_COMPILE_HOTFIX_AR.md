# إصلاح R18.3 — تجميع شاشة الإعدادات

أصلح هذا التحديث خطأ Java في `SettingsScreen.createView()` سببه تعريف متغيرين محليين بالاسم `coverage` داخل النطاق نفسه.

## التعديل

- أبقينا متغير تغطية مكتبات اللغات باسم `coverage`.
- غيّرنا متغير شارة اكتمال قطع القداس إلى `liturgyCoverageBadge`.
- لم تتغير النصوص أو البيانات اليومية أو التوقيعات الرقمية.
- أضيف اختبار تراجعي يمنع عودة الاسم المتكرر.

## الخطأ الذي يمنعه

```text
variable coverage is already defined in method createView()
```
