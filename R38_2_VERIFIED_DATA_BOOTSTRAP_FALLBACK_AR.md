# R38.2 — إصلاح توافق فرع verified-data القديم

## المشكلة
بعد تثبيت عقد النافذة على تسعة أيام، بقي فرع `verified-data` المنشور بتاريخ 2026-08-01 موقّعًا بالعقد السابق ذي 21 يومًا. كان Build يرفض الفرع قبل أن يحصل Update المجدول على فرصة لإعادة نشره، فتتوقف نسخة debug رغم أن المصدر والتوقيع صحيحان لكن العقد قديم.

## الإصلاح
- يبقى التحقق الصارم: حزم 21 يومًا لا تُستورد إلى التطبيق الجديد.
- لا تُقص الحزمة القديمة ولا يُعاد توقيعها داخل Build، لأن Build لا يملك مفتاح البيانات الخاص.
- عند اكتشاف عقد قديم في بناء debug، تُستخدم الحزمة المضمّنة الموقعة والمتحقق منها بدلًا منه.
- أول تشغيل مجدول لـ `Rolling Liturgical Window Update` يعيد إنشاء ونشر `verified-data` بعقد تسعة أيام.
- بناء الإصدار المحمي يبقى صارمًا ولا يستخدم هذا التجاوز؛ لا يمكن نشر Release قبل وجود فرع بيانات متوافق وحديث.

## الأمان
هذا الإصلاح لا يقبل حزمة 21 يومًا ولا يغيّر توقيعها. هو يمنعها من استبدال بيانات التطبيق مؤقتًا، ويستخدم فقط bootstrap موقّعًا اجتاز `verify_data_signature.py` و`validate_embedded_app_data.py`.
