# R31 — إصلاح اعتماد فحص منطقة أمان الأيقونة

## المشكلة

كان الاختبار `tests/test_icon_safe_zone.py` يستورد `PIL.Image`، بينما `Pillow` ليست ضمن `requirements-dev.txt`. لذلك فشل GitHub Actions أثناء جمع الاختبارات قبل تنفيذ الفحص نفسه.

## الحل

تم استبدال الاستيراد بقارئ PNG محدود وواضح يستخدم فقط `struct` و`zlib` من مكتبات Python القياسية. القارئ يقبل ملف الأيقونة المعتمد بصيغة PNG غير متداخلة، 8-bit RGBA، ويعيد بناء مرشحات PNG الخمسة ثم يحسب حدود قناة الشفافية.

## الضمانات المتبقية

- اللوحة يجب أن تكون `108×108`.
- الرسم غير الشفاف يجب ألا يتجاوز `66×66`.
- Adaptive Icon يجب أن يستخدم `church_prayers_cross_foreground`.
- لا توجد حاجة لإضافة Pillow إلى بيئة البناء.

## التحقق

- `python -S tests/test_icon_safe_zone.py`: ناجح دون site-packages.
- `pytest`: 379 اختبارًا و14 اختبارًا فرعيًا ناجحًا.
- جميع مراحل quality gate نُفذت بنجاح على دفعات بسبب حد زمن أداة التنفيذ المحلية.
