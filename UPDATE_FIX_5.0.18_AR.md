# إصلاح تحديث قراءات الأردن — Orthodox Prayers 5.0.18

التاريخ: 2026-07-29

## السبب المؤكد

تشغيل Rolling Week Update ينشر ملفات JSON والتواقيع إلى فرع `verified-data`، لكنه لا يحدّث ملف APK المثبت على الهاتف.
كما كشف سجل GitHub السابق فشلًا داخل شجرة النشر بسبب عدم وجود `validate_rolling_week.py` وقت التحقق النهائي.

## الإصلاحات

- رفع الإصدار إلى `5.0.18` ورقم البناء إلى `50018` حتى يقبل أندرويد تثبيته كتحديث جديد.
- نسخ مجلد `scripts/` كاملًا إلى شجرة النشر بدل قائمة ملفات قابلة للنقص.
- التحقق صراحة من وجود أدوات فحص rolling week والقارئ والـmanifest قبل النشر.
- إعادة فحص update manifest بعد تجميع شجرة النشر وقبل الدفع إلى `verified-data`.
- الاحتفاظ بتصنيف الخطأ الحقيقي عند حدوث استثناء غير متوقع بدل تحويله مباشرة إلى رسالة عامة.
- تحديث عقود الإصدار وSBOM ووكلاء الاتصال إلى 5.0.18.

## التحقق

- اختبارات الدفعة الأولى: 196/196 ناجحة.
- اختبارات الدفعة الثانية: 116/116 ناجحة.
- الإجمالي: 312/312 ناجحة.
- `scripts/validate_workflows.py`: ناجح.
- `scripts/verify_r20_release.py`: ناجح للإصدار 5.0.18.

## المطلوب على GitHub

ارفع محتويات المشروع إلى فرع `main`، ثم شغّل Workflow باسم `Build` أو اترك Push إلى main يشغّله تلقائيًا.
لا يكفي تشغيل `Rolling Week Update` وحده؛ فهو يحدّث البيانات ولا يبني APK.
بعد نجاح Build نزّل `app-debug.apk` من Artifact وثبته على الهاتف. رقم البناء 50018 يسمح بالتثبيت فوق 50017 عندما يكون توقيع APK متطابقًا.
