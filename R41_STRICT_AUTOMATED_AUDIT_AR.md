# R41 — التدقيق الآلي الصارم وحماية الروزنامة

## القيود المحفوظة

- الحد الأدنى بقي `minSdk = 26`، أي Android 8.0 فأحدث.
- لم يُعدّل أي ملف من ملفات الروزنامة السنوية 2026–2050.
- لم تُضعف حماية HTTPS أو التوقيع الرقمي أو فصل اللغات.
- لم تُضف مكتبات ثقيلة أو خدمات تتبع أو إعلانات.

## حماية الروزنامة

أُضيف الملف `canonical/calendar_2026_2050_lock.json` ببصمات SHA-256 وأحجام 28 ملفًا معتمدًا، منها المصدر الداخلي الكامل وفهرس السنوات وملفات كل سنة. ينفذ `scripts/validate_calendar_immutability.py` فحصًا حرفيًا قبل البناء والنشر، ويفشل عند:

- تغيير أي بايت أو حجم.
- حذف سنة أو ملف.
- إضافة ملف روزنامة غير مقفول.
- تغيير النطاق 2026-01-01 إلى 2050-12-31 أو عدد الأيام 9,131.

## تدقيق النصوص الدينية

ينفذ `scripts/validate_strict_religious_content.py` بوابة حاسمة لكل يوم ولكل لغة نشطة:

- وجود رسالة وإنجيل واحدين دون تكرار.
- صحة المرجع القانوني وترتيب نطاق الآيات.
- تطابق المرجع بين القراءة ودليل المصدر.
- مطابقة SHA-256 للنص المعروض حرفيًا.
- مطابقة عدد الأسطر/الآيات وبصمة كل واحد منها عند توفر دليل الآيات.
- وجود `source_id` و`source_url` وحالة مصدر أصلية مسموحة.
- منع AI translation وmachine translation وautomatic diacritization.
- منع النص المنتمي إلى لغة أخرى داخل المسار العربي أو الإنجليزي أو اليوناني.

## سياسة التذكار

أُضيفت `CommemorationDisplayPolicy` لتكون المصدر الوحيد لسلوك التذكار:

- التذكار الموثق وغير الفارغ يظهر.
- الحالة Pending أو Unavailable أو No Verified تختفي.
- العبارة العامة «تذكار اليوم بحسب التقويم الكنسي القديم» وما يقابلها بالإنجليزية واليونانية لا تظهر للمستخدم.
- عند عدم توفر النص في اللغة المختارة لا تظهر رسالة «الترجمة غير متوفرة» كتذكار.
- العيد الداخلي المثبت فعليًا يبقى ظاهرًا.

السياسة مطبقة في Home وCalendar وCalendar Day وUpcoming والتنبيه والـWidget.

## الأداء والوصول

- أُلغي تحميل سنة الروزنامة عند إنشاء المستودع؛ يتم التحميل عند فتح التقويم أو طلب تاريخ محدد فقط.
- عند ضغط الذاكرة يُحرر فهرس البحث، وعند خروج الواجهة تُحرر سنة التقويم المحملة، مع إبقاء بيانات اليوم الموقعة.
- رُفعت مساحة لمس زر الرجوع الأساسية إلى 48×48dp.

## نتيجة التحقق المحلي

- نجحت بوابة الجودة الكاملة و451 اختبار Python و14 اختبارًا فرعيًا.
- نجح قفل الروزنامة لجميع 9,131 يومًا حتى 2050.
- نجح التدقيق الصارم للرسالة والإنجيل في العربية والإنجليزية واليونانية.
- نجح فحص `javac` المستقل لسياسة التذكار الجديدة.
- تعذر تنفيذ Gradle محليًا لأن بيئة التنفيذ لم تستطع الوصول إلى `services.gradle.org` لتنزيل Gradle 8.13؛ GitHub Build يبقى مسؤولًا عن Java/Android unit tests وLint وAPK.
