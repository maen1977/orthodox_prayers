# تقرير تنفيذ المرحلة السابعة R21

## النتيجة التنفيذية

فعّلت المرحلة السابعة مسارًا آمنًا ومستقلًا لاستيراد الطبعات الأصلية الكاملة لقداس القديس باسيليوس الكبير وخدمة القداس السابق تقديسه. لا يُسمح لأي مرشح بالظهور داخل التطبيق، ولا تتم الترقية إلا عند اكتمال اللغات العربية والإنجليزية واليونانية معًا، ومراجعة كل فقرة مقابل المصدر، وتسجيل موافقة كنسية وبصمة ثابتة للمرشح.

لم تُنشر نصوص ناقصة أو مشوهة، ولم يُستخدم قداس يوحنا الذهبي الفم بديلًا عن خدمة أخرى.

## المصدر العربي للسابق تقديسه

تم الحصول على الطبعة العربية الرسمية المنشورة من مطرانية الروم الأرثوذكس في الأردن:

- 152 صفحة.
- الطبعة الخامسة، 2018.
- بصمة الملف: `ace55aed85ca4ac9437bae5e7d3ebe2baab1a7cf4480c481602a64425077907e`.
- الملف الأصلي بقي خارج المشروع؛ المشروع يحتفظ بالبصمة ودليل الفحص فقط.

أظهر فحص طبقة النص وجود **8,482** محرف استبدال Unicode بسبب ترميز الخط القديم داخل ملف PDF. لذلك حُظر النص آليًا من الاستيراد والنشر. لم تُستخدم OCR ولا محاولة تخمين أو تصحيح آلي؛ الطريق الآمن هو الحصول على ملف Word الرسمي أو تفريغ Unicode مطابق مع مراجعة صفحة بصفحة.

## الأدوات المضافة

### `scripts/import_native_liturgy_service.py`

- يستورد `DOCX` أو `HTML` أو `TXT` أو PDF ذي طبقة Unicode سليمة.
- يتحقق من اللغة ونسبة الكتابة وعدد الفقرات والحجم وعلامات الخدمة الأساسية.
- يرفض أحرف الاستبدال، المحارف غير الآمنة، اختلاط الكتابات، وضياع حدود الكلمات العربية.
- ينشئ مرشحًا غير قابل للعرض يحمل بصمات المصدر والنص.
- لا يترجم ولا يصحح ولا ينشر.

### `scripts/promote_native_liturgy_service.py`

- يرفض الترقية إذا غابت أي لغة من اللغات الثلاث.
- يشترط حالة مراجعة `APPROVED` واسم المراجع والتاريخ والتحقق من الصفحات أو الفقرات.
- يتحقق من بصمة المرشح بعد المراجعة.
- يرقّي اللغات الثلاث بصورة ذرية فقط.
- لا يحدّث بصمات النصوص ولا يوقّع بيانات الإصدار تلقائيًا؛ تبقى هاتان خطوتين منفصلتين للمراجعة والإصدار.

### `scripts/validate_native_liturgy_imports.py`

- يتحقق من عقود المصادر الرسمية لكل لغة.
- يثبت أن المرشحات غير قابلة للعرض.
- يثبت عدم تسرب خدمة باسيليوس أو السابق تقديسه غير المراجعة إلى حزم التطبيق.
- يتحقق من دليل ملف السابق تقديسه العربي وحالة فساد الاستخراج.
- أضيف إلى بوابة الجودة الشاملة.

## التغييرات في واجهة الخدمة

عندما يعيّن التقويم باسيليوس أو السابق تقديسه ولا تتوفر الطبعة المكتملة:

- لا يظهر قالب يوحنا الذهبي الفم.
- تظهر حالة الخدمة المطلوبة.
- تظهر حالة استيراد الطبعة الأصلية بلغة المستخدم.
- تبقى جميع خانات الصلاة الديناميكية غائبة بدل ملئها بنص من طقس آخر.

## الاختبارات

- جميع اختبارات المشروع: **228 ناجحًا، صفر فشل**.
- اختبارات المرحلة السابعة: **10 من 10**.
- بوابة الجودة الكاملة مع `--strict-native-lanes`: ناجحة.
- العربية: `656/656`.
- الإنجليزية: `785/785`.
- اليونانية: `777/777`.
- الملفات الدينية والعقود المحمية بالبصمات: **27 ملفًا**.
- التوقيعات الحالية وبيانات 26 تموز 2026 لم تتغير.
- لم يتسرب أي مرشح غير مراجع إلى بيانات Android.

## بناء Android

لم يُنشأ APK. حاول Gradle تنزيل الإصدار 8.13، لكن بيئة التنفيذ لم تتمكن من الوصول إلى `services.gradle.org` وظهر `UnknownHostException`. فحص Gradle Wrapper وبصمته نجح، أما التجميع الفعلي فلم يتم.

## حالة الاكتمال بعد المرحلة السابعة

اختيار نوع القداس آمن، ومسار استيراد النصوص صار جاهزًا ومحكومًا بالمراجعة، لكن النصوص الكاملة لطقسي باسيليوس والسابق تقديسه **لم تصبح قابلة للعرض بعد**:

- باسيليوس العربي: يلزم ملف مصدر عربي كامل معتمد.
- باسيليوس الإنجليزي واليوناني: المصادر الرسمية مسجلة، والاستيراد الحرفي والمراجعة ما زالا مطلوبين.
- السابق تقديسه العربي: المصدر الرسمي موجود، لكن طبقة النص في PDF غير صالحة؛ يلزم Word رسمي أو تفريغ Unicode مراجع.
- السابق تقديسه الإنجليزي واليوناني: المصادر الرسمية مسجلة، والاستيراد والمراجعة ما زالا مطلوبين.

لذلك بقيت حالة الخدمتين `displayable=false`، وبقي ادعاء اكتمال القداس السنوي محجوبًا.
