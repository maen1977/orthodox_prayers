# تقرير تنفيذ المرحلة الثالثة R21

## الحالة

تم تنفيذ المرحلة الثالثة على نسخة **5.0.16 / R21 Phase 2**. هذه المرحلة تبني مسار النصوص المتغيرة حسب اليوم، لكنها **لا تعلن أن القداس مكتمل بكل فروعه** ولا تنشر بيانات يومية غير موقعة.

## ما تم تنفيذه

1. إضافة سجل `canonical/resurrectional_propers.json` للألحان القيامة الثمانية بالعربية والإنجليزية واليونانية.
   - كل نص مستقل في لغته.
   - لكل نص بصمة SHA-256.
   - لا توجد ترجمة آلية أو تعويض من لغة أخرى.
   - وُسم السجل بأنه يحتاج مراجعة كنسية بشرية نهائية قبل الاعتماد الإنتاجي.

2. إضافة سجل الإيوثينا الأحد عشر مع مراجعها القانونية، ومن ضمنها الإيوثينا الثامنة: يوحنا 20:11-18.

3. إضافة سجل مؤرخ رسمي ليوم **الأحد 26 تموز 2026** يتضمن:
   - الأحد الثامن بعد العنصرة.
   - اللحن السابع.
   - الإيوثينا الثامنة.
   - إنجيل السحر: يوحنا 20:11-18.
   - الرسالة: 1 كورنثوس 1:10-17.
   - الإنجيل: متى 14:14-22.
   - طروبارية القيامة، القنداق، البروكيمنن وآية المناولة في اللغات الثلاث.

4. تعديل `scripts/update_liturgical_data.py` بحيث:
   - يبحث أولًا عن سجل مؤرخ وموثق.
   - يستخدم سجل العيد الثابت بعده.
   - يستخدم لحن القيامة للأحد العادي عند عدم وجود سجل مؤرخ.
   - يخفي القنداق أو طروبارية الكنيسة أو أي قطعة غير موثقة بدل اختراعها أو عرض ملاحظة بديلة.
   - يضيف `resurrection_tone` و`eothinon` و`proper_id` إلى عقد خدمة اليوم.

5. إنشاء مرشح خدمة يومية:
   - الملف: `R21_PHASE3_CANDIDATE_2026-07-26.json`.
   - جميع الخانات المطلوبة ممتلئة بالعربية والإنجليزية واليونانية باستثناء `church_troparion`، وهي مخفية عمدًا لأن تكريس الكنيسة أو طروبارية شفيعها لم يُحدد بمصدر.
   - المرشح موسوم `do_not_publish=true`.

6. إضافة ستة اختبارات جديدة تمنع:
   - فقدان أحد الألحان الثمانية.
   - فقدان أحد مراجع الإيوثينا.
   - الرجوع إلى اكتشاف شبكي غير موثوق عند وجود سجل رسمي مؤرخ.
   - ظهور خانة يومية أساسية فارغة ليوم 26 تموز 2026.
   - اختراع قنداق للأحد العادي.
   - إعلان اكتمال القداس الرئيسي قبل استيفاء بقية الفروع.

## نتائج الفحص

- اختبارات المشروع: **189 ناجحًا، صفر فشل**.
- اختبارات المرحلة الثالثة: **6 ناجحة، صفر فشل**.
- التوقيعات الموجودة للبيانات المنشورة السابقة: ناجحة ولم تتغير.
- فصل اللغات وعدم الترجمة الآلية: ناجح.
- خدمة الأردن والتقويم والصوم والمصادر: ناجحة.
- فحص عدم وجود ملاحظات عامة بدل الصلاة: ناجح.
- بوابة الجودة المجمعة وصلت إلى مهلة بيئة التنفيذ بعد فحص إزالة التكرار، ثم شُغلت جميع الفحوص المتبقية منفردة ونجحت.

## التوقيع والنشر

المشروع يحتوي مفتاح التحقق العام، لكنه لا يحتوي مفتاح الإصدار الخاص. لذلك:

- لم يُنشأ مفتاح جديد.
- لم تتغير هوية التوقيع.
- لم تُستبدل ملفات `today.json` الموقعة بمرشح غير موقع.
- يجب توقيع المرشح بالمفتاح الرسمي الموجود لدى صاحب المشروع قبل إدخاله إلى مسار النشر.

## بناء أندرويد

فشل تشغيل `./gradlew test --no-daemon` قبل بدء البناء لأن البيئة لم تستطع الوصول إلى `services.gradle.org` لتنزيل Gradle 8.13. فحص ملفات Gradle Wrapper وبصمة التوزيعة نجح، لكن APK لم يُبنَ في هذه البيئة.

## ما بقي قبل اعتبار القداس كاملًا بكل فروعه

- تغطية الميناون والتريودي والبندكستاري لكل أيام السنة.
- آيات هلليلويا اليومية والبدائل الكاملة للأنتيفونات والإيصوديكون.
- `واجب الاستئهال` وبدائلها، الميغاليناريون، والصرف المتغير كاملًا.
- ضبط طروبارية شفيع أو تكريس كل كنيسة.
- تفسير آبائي موثق لكل إنجيل دون نسبة نص إلى قديس بلا مرجع.
- قداس باسيليوس وخدمة السابق تقديسه كخدمتين مستقلتين كاملتين.
- مراجعة كنسية بشرية نهائية للألحان الثمانية في اللغات الثلاث.
- التوقيع الرسمي وبناء أندرويد النهائي.

## نتيجة المرحلة

المرحلة الثالثة **مكتملة برمجيًا ضمن نطاقها**: صار البرنامج قادرًا على تركيب خدمة الأحد المؤرخة من مصادر مسجلة، مع إنجيل السحر والقطع والقراءات اليومية، ويغلق الخانات غير الموثقة بدل تخمينها. أما عبارة **القداس مكتمل بكل فروعه** فما زالت محجوبة بصدق إلى أن تنتهي البنود المذكورة أعلاه.
